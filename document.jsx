/**
 * @module Date-range-picker 
 */

export default class DateRangePicker {
    /** 
    * <div class="exapmle" data-module="dateRangePicker" data-example='[{"all":"890-780"}]'></div>
    * @property {string}  startValue	                 - 	Define the elements start to value, and endValue used together.
    * @property {string}  endValue                  - Define the elements end to value, and startValue used together.
    * @property {string}  maxDate             - Define elements the date of the biggest.
    * @property {string} minDate                -	Define elements the date of the minimum.
    * @property {string}  separator              - 	Define the elements of the separator, the default is “~”.
    * @property {string}  dateLimit              - 	Define the elements of the limit of date
    * @property {enum}  open              - 	Define the datePicker open direction.
    * @property {enum}  timePicker              - Defining whether show the timePicker.
    * @property {string}  startValueLink              -Provide the bidirectional binding for the start date.
    * @property {string}  endValueLink              - Provide the bidirectional binding for the end date.
    * @property {boolean}  manualInput              - 	whether the input can be modified manually, default false.
    * @property {string}  id              - 		Uniquely identifies an element.
    * @property {string}  label              -Defining the elements label.
    * @property {string}  value              -Defining the value of an element.
    * @property {shape}  valueLink             - Provide the bidirectional binding for data.
    * @property {string} defaultValue                -	Define the elements of the default values.
    * @property {string}  io              - 		Define the elements as input or output,the default is 'in'.
    * @property {string}  placeHolder              - 	Define the elements input fields of expectations,the default is '==Please Input=='
    * @property {string}  helpText              - Define the elements with the help of the message.
    * @property {string}  format	              - Define the elements of the format.
    * @property {string}  mask              -	Define the elements cover index,if it is “#:(3,7)”,the results for 123####8888.“:”before the arbitrary symbols can substitute for cover character,“()” fill in the index.if it is “#:(7)”,the results for 1236666####.if it is “*：(-1)”,the results for 1**********..
    * @property {css}  style              - 	Defining the elements of the CSS style, such as {{width:'10px'}}.
    * @property {enum}  styleClass              - 	Define the style of an element,it can be one of default,primary,success,warning,danger,info.
    * @property {string}  widthAllocation              - 	Defining lable and input field proportion, divided into 12 parts, the default is '4,8'.
    * @property {boolean}  required	              	Define the elements are required,the default is false.
    * @property {boolean}  enabled              -	Defines whether the element can be input,the default is true.
    * @property {boolean}  visibled              - Defines whether the element can be hidden,the default is true.
    * @property {string}  validationGroup	              - Defines a set of elements for the validation specified, between groups can be separated by a comma.
    * @property {string}  validationLabel              -		the label which used when validate
    * @property {number}  maxLength              -Define the elements check the maximum length of the input value.
    * @property {number}  minLength              -Define the elements validation minimum length of the input value.
    * @property {string}  maxLengthMessage              - 	Define the elements checking the input value is greater than the maximum length of message.
    * @property {string}  minLengthMessage	            -  Define the elements checking the input value is less than the minimum length of messag.
    * @property {string}  requiredMessage              -	Define the elements check the required message of the input value.
    * @property {string}  layout              -		Defining the elements of the arrangement, the default is horizontal.( horizontal/vertical)
    * @property {func}  onChange              - 	Define the elements in the object changes trigger events.
    * @property {func}  onBlur	              - 	Define the elements in the triggering event object loses focus.
    * @property {func} onFocus              -	 Define the elements in the object gains focus when the triggering event
    * @property {string}  colspan	              -	Defining the elements colspan cols.
    */
    example() {

    }
};



